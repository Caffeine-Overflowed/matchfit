query notifications($params: GetNotificationsInput) {
  notifications(params: $params) {
    items {
      ...NotificationType
    }
    totalCount
    hasMore
  }
}

query unreadNotificationsCount {
  unreadNotificationsCount
}

